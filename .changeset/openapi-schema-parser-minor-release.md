---
"@asyncapi/openapi-schema-parser": minor
---

Publish a new release from the `parser-js` monorepo. Version `3.0.25` / `3.0.26` cannot be republished on npm (previously used), so this bumps to the next minor (`3.1.0`).
