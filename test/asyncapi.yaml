asyncapi: '2.0.0-rc1'
id: 'urn:myapi'
info:
  title: My API
  version: '1.0.0'
channels:
  mychannel:
    publish:
      traits:
        - $ref: '#/components/traits/docs'        
      externalDocs:
        x-extension: true
        url: 'https://irrelevant.com'
      message:
        $ref: '#/components/messages/testMessage'

components:
  messages:
    testMessage:
      traits:
        - $ref: '#/components/traits/no-params'
      payload:
        $ref: '#/components/schemas/testSchema'
  schemas:
    testSchema:
      type: object
      properties:
        name:
          type: string
        test:
          $ref: 'refs/refed.yaml'
  traits:
    no-params:
      x-some-extension: 'some extension'
    docs:
      externalDocs:
        url: https://company.com/docs