import {ServerV3} from '../../../src/models';

describe('Server model', function () {
    describe('.url', function () {
        it('should return the url', function () {
            const doc = { url: "https://github.com/asyncapi" };
        })
    })
})